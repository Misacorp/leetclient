import styled from 'styled-components';

/**
 * Narrow container
 */
const NarrowContainer = styled.div`
  max-width: 500px;
  margin-left: auto;
  margin-right: auto;
  padding: 0.5rem;
`;

export default NarrowContainer;
